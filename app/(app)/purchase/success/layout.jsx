// app/(app)/purchase/success/layout.jsx
// Layout vacío para esta ruta: sin header, sin sidebar, etc.
export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function SuccessLayout({ children }) {
  return children;
}
